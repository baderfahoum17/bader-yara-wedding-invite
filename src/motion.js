// Shared easing curves.
// ease: exponential ease-out, the default for every reveal and transition.
// settle: a slight overshoot that lands, for the canvas arriving under the cover.
export const ease = [0.16, 1, 0.3, 1]
export const settle = [0.32, 1.12, 0.38, 1]
